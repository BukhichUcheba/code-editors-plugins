import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2', // Основной цвет (синий)
      dark: '#115293', // Темный оттенок
    },
  },
});

export default theme;